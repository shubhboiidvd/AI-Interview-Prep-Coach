export const selectSession = (state) => state.session;
export const selectCurrentSession = (state) => state.session.currentSession;
export const selectCurrentQuestion = (state) => state.session.currentQuestion;
export const selectChatHistory = (state) => state.session.chatHistory;
